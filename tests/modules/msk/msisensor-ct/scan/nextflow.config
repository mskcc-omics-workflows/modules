params {
    enable_conda = false
    fasta = "/home/buehlere/access_nextflow/getbasecountmultisample/chr22.fasta"
    outdir = "output"
}

process {

    publishDir = { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" }

}